{% extends 'base.html.twig' %}

{% block title %}Section Films {% endblock %}

{% block body %}
    <h1 class="mt-4 text-center"> Films

        <div class="row">
     {% for film in films %}
         <div class="col-md-4">
             <div class="product-item text-center">
                 <a href="{{ path('app_product_show', { 'slug' : film.slug }) }}"><img src="/uploads/{{ film.illustration }}" alt="{{ film.name }}" class="img-fluid" width="150px"></a>
                 <h4>{{ film.name }}</h4>
                 <span class="product-subtitle mt-2"> {{ film.subtitle }}</span>
                 <span class="product-price mt-4"><b> {{ film.price }} € </b></span>
             </div>
         </div>
     {% endfor %}
        </div>
{% endblock %}