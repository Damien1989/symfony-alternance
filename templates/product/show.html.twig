{% extends 'base.html.twig' %}

{% block title %} {{ product.name }} {% endblock %}

{% block body %}

    <div class="row">
        <div class="col-md-5">
            <a href="{{ path ('product', {'slug' : product.slug}) }}"></a> <img src="/uploads/{{ product.picture }}" alt=" {{ product.name }}" class="img-fluid">
        </div>
        <div class="col-md-7 my-auto">
            <h3> {{ product.name }}</h3>
            <span class="product-page-price">{{ (product.price) }} €</span>
            <hr>
            <p>
                {{ product.description }}
            </p>
            <a href="" class="btn btn-primary">Ajouter au panier</a>
        </div>
    </div>

{% endblock %}